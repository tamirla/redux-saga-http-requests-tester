// these are just the identifiers given to each api (NOT the urls),
// so they can be used to define the api, dispatch it & watch for its completion\failure
export const GET_TODOS = 'getTodos';
export const GET_USER = 'getUser';
export const ADD_POST = 'addPost';
export const INVALID_API = 'invalid';
